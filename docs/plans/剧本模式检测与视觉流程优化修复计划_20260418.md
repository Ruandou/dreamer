# 剧本模式检测与视觉流程优化修复计划\_20260418

## 问题清单

### P0（阻塞性）

1. **完整剧本被当创意处理** - 用户上传36集剧本，AI重新生成
2. **视觉补全JSON解析失败** - DeepSeek返回非法JSON导致解析中断

### P1（体验优化）

3. **visualStyleConfig需手动生成** - 应在解析时自动生成
4. **visualStyle字段冗余** - visualStyleConfig已完全覆盖其功能

---

## 修复方案

### 修复1：runGenerateFirstEpisode 检测完整剧本

**文件**：`packages/backend/src/services/project-script-jobs.ts:160-181`

**问题**：用户上传完整剧本到description，但runGenerateFirstEpisode直接当创意处理

**修复**：

```typescript
export async function runGenerateFirstEpisode(projectId: string) {
  const project = await projectRepository.findUniqueById(projectId)
  if (!project) throw new Error('PROJECT_NOT_FOUND')

  const idea = project.description?.trim() || project.name

  // ✅ 检测是否为完整剧本
  const detectionResult = detectScriptMode(project.description || '')

  if (detectionResult.mode === 'faithful-parse' || detectionResult.mode === 'mixed') {
    // 完整剧本 → 存 synopsis，抛异常引导解析
    await projectRepository.update(projectId, {
      synopsis: project.description,
      storyContext: project.description.slice(0, 12000)
    })

    throw new Error('SHOULD_PARSE_SCRIPT_INSTEAD')
  }

  // 创意 → 正常AI生成
  const { script } = await writeScriptFromIdea(idea, {...})
  // ...
}
```

---

### 修复2：前端处理 SHOULD_PARSE_SCRIPT 错误

**文件**：`packages/frontend/src/views/Generate.vue:233-259`

```typescript
async function runGenerateFirstEpisode() {
  try {
    await api.post(`/projects/${id}/episodes/generate-first`, {})
    await loadProject(id)
    message.success('第一集剧本已生成')
  } catch (e: any) {
    // ✅ 处理完整剧本检测
    if (e.message?.includes('SHOULD_PARSE_SCRIPT')) {
      message.warning('检测到完整剧本，正在跳转到项目详情页...')
      setTimeout(() => router.push(`/project/${id}`), 1500)
      return
    }
    message.error(e.message || '生成失败')
  } finally {
    isGeneratingFirst.value = false
  }
}
```

---

### 修复3：解析剧本时自动生成 visualStyleConfig（核心）

**文件**：`packages/backend/src/services/project-script-jobs.ts` - runParseScriptJob 函数

**时机**：在解析剧本开始时，提取角色/场景之前

**原因**：

- 此时已有完整 synopsis（故事梗概）
- 此时已有所有 Episode.script（分集剧本）
- AI有足够信息做出最佳视觉分析
- 生成的 visualStyleConfig 可立即用于角色/场景 imagePrompt

```typescript
export async function runParseScriptJob(jobId: string, projectId: string, targetEpisodes: number) {
  const project = await projectRepository.findFirstOwnedWithEpisodesEp1(projectId, userId)

  // ✅ 在解析开始时，基于完整梗概生成 visualStyleConfig
  if (!project.visualStyleConfig) {
    console.log('[parse-script] 基于完整梗概自动生成 visualStyleConfig')
    try {
      const config = await generateVisualStyleConfig({
        name: project.name,
        description: project.description,
        synopsis: project.synopsis  // ✅ 此时已有完整梗概
      }, {
        userId: project.userId,
        projectId,
        op: 'auto_generate_visual_style'
      })

      await projectRepository.update(projectId, { visualStyleConfig: config })
      console.log('[parse-script] visualStyleConfig 已生成并保存')
    } catch (error) {
      console.error('[parse-script] 自动生成 visualStyleConfig 失败:', error)
      // 不阻断流程，继续解析
    }
  }

  // 然后继续：提取角色、场景（使用刚生成的 visualStyleConfig）
  await applyScriptVisualEnrichment(projectId, {...})
  // ...
}
```

---

### 修复4：移除 visualStyle 前置检查

**文件**：`packages/backend/src/services/project-service.ts:193`

**问题**：当前要求用户手动选择 visualStyle 才能解析

**修复**：visualStyleConfig 会在解析时自动生成，不需要前置检查

```typescript
// 旧：
if (!project.visualStyle?.length) {
  return { ok: false, status: 400, error: '请至少选择一种视觉风格' }
}

// 新：
// ✅ 移除检查，visualStyleConfig 会在 runParseScriptJob 中自动生成
// 如果生成失败会捕获，不会阻断流程
```

---

### 修复5：增强JSON解析容错

**文件**：`packages/backend/src/services/script-visual-enrich.ts:297-313`

**问题**：DeepSeek返回非法JSON直接失败

**修复**：AI自动修复JSON语法错误

```typescript
let payload: VisualPayload
try {
  const raw = extractJsonObject(jsonText)
  payload = JSON.parse(raw) as VisualPayload
} catch (e) {
  // ✅ 尝试AI修复JSON
  console.warn('[visual-enrich] JSON解析失败，尝试自动修复...')
  try {
    const fixedJson = await repairJsonWithAI(jsonText, {
      userId: projectRow.userId,
      projectId,
      op: 'visual_enrich_json_repair'
    })
    payload = JSON.parse(fixedJson) as VisualPayload
    console.log('[visual-enrich] JSON修复成功')
  } catch (repairError) {
    throw new Error(`视觉补全失败：JSON解析和修复均失败`, { cause: e })
  }
}
```

**新增文件**：`packages/backend/src/services/ai/json-repair.ts`

```typescript
import { callLLMWithRetry } from './llm-call-wrapper.js'
import { getDefaultProvider } from './llm-factory.js'
import type { ModelCallLogContext } from './api-logger.js'

const REPAIR_PROMPT = `修复下列JSON的语法错误，只返回修复后的JSON，不要解释：

规则：
1. 数组元素间必须有逗号
2. 键名必须用双引号
3. 不能有尾随逗号
4. 保持原内容不变，只修复语法`

export async function repairJsonWithAI(
  brokenJson: string,
  log?: ModelCallLogContext
): Promise<string> {
  const provider = getDefaultProvider()

  const result = await callLLMWithRetry(
    {
      provider,
      messages: [
        { role: 'system', content: REPAIR_PROMPT },
        { role: 'user', content: brokenJson }
      ],
      temperature: 0.1,
      maxTokens: 10000,
      modelLog: log
    },
    (content) => content.trim()
  )

  return result.content
}
```

---

### 修复6：优化评分标准

**文件**：`packages/backend/src/services/project-script-jobs.ts:1088-1107`

**问题**：剧本检测阈值过严，容易误判

**修复**：

```typescript
export function calculateOverallScore(script: string): number {
  let score = 0

  // 分集标记 - 3分（原2分）
  const episodeMatches = script.match(/第\s*\d+\s*集/gi) || []
  if (episodeMatches.length >= 3) score += 3
  else if (episodeMatches.length >= 1) score += 2

  // 场景标记 - 2分
  if (/第?\d+[场景场]|scene\s*\d*/i.test(script)) score += 2

  // 对话格式 - 2分
  if (/[""「"]|[\u4e00-\u9fa5]+[：:]\s*[""「"]/.test(script)) score += 2

  // 字数 - 1分（阈值提高到5000）
  if (script.length > 5000) score += 1

  return score  // 满分10分
}

// detectScriptMode 阈值从6降到5
if (overallScore >= 5) {
  return { mode: 'faithful-parse', ... }
}
```

---

### 修复7：前端移除 visualStyle 选择UI

**文件**：`packages/frontend/src/views/Generate.vue`

**删除**：

- 第700-712行：视觉风格checkbox区域
- 第206-214行：toggleStyle/styleActive 函数
- 第403-406行：visualStyle 检查

**修改 runParse**：

```typescript
async function runParse() {
  const id = projectId.value
  if (!id) return

  // ✅ 移除 visualStyle 检查，直接调用解析
  // 后端会自动生成 visualStyleConfig

  isParsing.value = true
  try {
    const te = Math.max(
      MIN_TARGET_EPISODES,
      Math.min(MAX_TARGET_EPISODES, Math.floor(Number(targetEpisodeCount.value) || 1))
    )
    const { data } = await api.post<{ jobId: string }>(`/projects/${id}/parse`, {
      targetEpisodes: te
    })
    router.push(`/project/${id}?parseJobId=${data.jobId}`)
  } catch (e: any) {
    message.error(e?.response?.data?.error || e?.message || '解析失败')
  } finally {
    isParsing.value = false
  }
}
```

---

## 实施顺序

1. **修复6** - 优化评分标准（2分钟）
2. **修复1** - runGenerateFirstEpisode 检测（10分钟）
3. **修复2** - 前端错误处理（5分钟）
4. **修复3** - 解析时自动生成 visualStyleConfig（10分钟）
5. **修复4** - 移除前置检查（2分钟）
6. **修复5** - JSON容错（15分钟）
7. **修复7** - 前端UI清理（10分钟）
8. **测试** - 端到端验证（15分钟）

**总计**：~69分钟

---

## 测试验证

### 测试1：完整剧本上传

```bash
1. 上传 短剧剧本_天工开物_36集_改造方案.md
2. 观察提示"检测到完整剧本，正在跳转..."
3. 自动跳转到 /project/:id
4. 点击"解析剧本"
5. 检查 visualStyleConfig 是否自动生成
6. 检查任务类型为 parse-script
7. 检查角色/场景是否使用了 visualStyleConfig
```

### 测试2：创意输入

```bash
1. 输入"现代工程师穿越到古代造机器"
2. 正常生成第一集
3. 批量生成剩余集
4. 点击"解析剧本"
5. 检查 visualStyleConfig 自动生成
```

### 测试3：JSON解析失败修复

```bash
1. 模拟 DeepSeek 返回非法JSON（缺逗号）
2. 观察自动触发AI修复
3. 修复后成功落库
4. 检查日志中有 [visual-enrich] JSON修复成功
```

---

## 向后兼容

**保留 visualStyle 字段**（不删除数据库列），但：

- 不再在UI上显示
- 不再作为解析前置条件
- 代码中作为 fallback（如果 visualStyleConfig 生成失败）

```typescript
// script-visual-enrich.ts 中已有fallback
const projectVisualStyleLine = (projectRow.visualStyle || []).join('、') || '（未指定）'
```

---

**优先级**：🔴 P0  
**预计时间**：69分钟  
**影响范围**：前端Generate.vue、后端project-script-jobs.ts、script-visual-enrich.ts、project-service.ts
